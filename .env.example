# URL of the Apps Script web-app deployment that owns the target sheet.
# Deploy the script in apps-script/Code.gs to get this URL.
WEBHOOK_URL=

# Shared-secret token. Must match the TOKEN value stored in the
# Apps Script's Project Settings → Script Properties. The script
# rejects all requests unless this matches — secure by default.
WEBHOOK_TOKEN=
