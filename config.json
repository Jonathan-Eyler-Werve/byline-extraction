{
  "_note": "YOUR CONTENT HERE — replace the example feeds below with the pages you want to scrape. Each feed needs a pageUrl plus either a linkSelector (CSS selector for article links) or a linkPattern (regex applied to link URLs). The title becomes the feed_title column in the sheet.",
  "feeds": [
    {
      "title": "Example (CSS selector)",
      "pageUrl": "https://example.com/news/",
      "linkSelector": ".content a"
    },
    {
      "title": "Example (URL pattern)",
      "pageUrl": "https://example.com/topics/widgets/",
      "linkPattern": "https://example\\.com/articles/.+"
    }
  ]
}
